# 新規アカウント登録（オンボーディング）システム タスクリスト

- [x] 1. 新規登録コンポーネント (`SignupTown.tsx`) の作成
  - [x] ステップ入力UI（町内会情報、役員情報、確認）の実装
  - [x] 進行状況を示すプログレスバーの追加
- [x] 2. `Supabase` インサート処理の実装
  - [x] `SignupTown.tsx` の完了ステップで、`neighborhoods` テーブルへ新しい町内会データを `INSERT` する処理の作成
  - [x] 同時に（本番運用を見据えて）役員の `id` 等を保存するような基礎作り
- [x] 3. エントリポイント (`page.tsx`) の改修
  - [x] ログイン画面に「新規利用のお申し込み（役員向け）」ボタンを追加
  - [x] `page.tsx` の View State に `signup` を追加し、登録完了後にそのまま管理者画面 (`AdminView`) に自動ログイン・遷移する導線を実装
- [x] 4. 検証とウォークスルーの作成
