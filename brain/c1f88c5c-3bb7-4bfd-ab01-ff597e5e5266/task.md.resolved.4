# ページネーション・読み込み制限機能の実装

# ページネーション・読み込み制限機能の実装

- [x] `ResidentView.tsx`（スマホ側：「もっと見る」方式）の改修
  - [x] 初期表示を10件に制限（`pageSize = 10`）し、現在の読み込み上限数をStateで管理する。
  - [x] カテゴリー（タブ）切り替え時に、ローカルのフィルタリングではなくSupabaseに対する通信（絞り込み）を行うように修正する。（これにより「イベントタブ」にしたあとの件数が正しく10件担保される）
  - [x] 画面の最下部に「さらに読み込む」ボタンを設置し、押下時に表示件数の上限を拡張して再取得する処理を実装。
- [x] `AdminView.tsx`（PC側：ページャー方式）の改修
  - [x] 初期表示を10件に制限。ページ番号を管理するStateを追加。
  - [x] Supabaseの通信で全体の件数（`count: 'exact'`）と対象ページのデータ（`range`）を分けて取得するよう修正。
  - [x] 画面上部のサマリー用（総数）は別途取得するか、`count`の件数を利用する。
  - [x] 表（テーブル）の下部に「＜ 前へ」「次へ ＞」ボタンを追加し、ページ切り替え時にデータを再取得する。
