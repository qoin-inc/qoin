# Stripe Connect Express Integration Tasks

- [ ] バックエンドAPI実装
  - [ ] `src/app/api/admin/stripe/create-account-link/route.ts` を作成する
    - [ ] `neighborhoods`, `neighborhood_admins` から情報を取得
    - [ ] Expressアカウント未作成の場合は `stripe.accounts.create` で作成し、名前とメアドをプリフィルする
    - [ ] `neighborhoods.stripe_account_id` に保存する処理を実装
    - [ ] `stripe.accountLinks.create` でURLを発行して返す
  - [ ] `src/app/api/webhooks/stripe/route.ts` を作成する
    - [ ] `Stripe-Signature` の検証
    - [ ] `account.updated` の受け皿となる最低限の処理（ログ出力や将来のDB更新用）
- [ ] フロントエンドUI/UX実装
  - [ ] 役員ダッシュボード (`src/components/AdminView.tsx` または関連設定画面) にStripe登録ボタンを追加する
    - [ ] APIをコールし、戻り値のURLに対してリダイレクト処理（`window.location.href` 等）を実行する
  - [ ] `src/app/admin/stripe/return/page.tsx` を作成する（完了後の戻り先）
  - [ ] `src/app/admin/stripe/refresh/page.tsx` を作成する（中断時の戻り先）
- [ ] テストと検証
  - [ ] LintやTypeScriptエラーが出ないか確認する
