# Stripe Connect Express Integration Tasks

- [x] バックエンドAPI実装
  - [x] `src/app/api/admin/stripe/create-account-link/route.ts` を作成する
    - [x] `neighborhoods`, `neighborhood_admins` から情報を取得
    - [x] Expressアカウント未作成の場合は `stripe.accounts.create` で作成し、名前とメアドをプリフィルする
    - [x] `neighborhoods.stripe_account_id` に保存する処理を実装
    - [x] `stripe.accountLinks.create` でURLを発行して返す
  - [x] `src/app/api/webhooks/stripe/route.ts` を作成する
    - [x] `Stripe-Signature` の検証
    - [x] `account.updated` の受け皿となる最低限の処理（ログ出力や将来のDB更新用）
- [x] フロントエンドUI/UX実装
  - [x] 役員ダッシュボード (`src/components/AdminView.tsx` または関連設定画面) にStripe登録ボタンを追加する
    - [x] APIをコールし、戻り値のURLに対してリダイレクト処理（`window.location.href` 等）を実行する
  - [x] `src/app/admin/stripe/return/page.tsx` を作成する（完了後の戻り先）
  - [x] `src/app/admin/stripe/refresh/page.tsx` を作成する（中断時の戻り先）
- [x] テストと検証
  - [x] LintやTypeScriptエラーが出ないか確認する
